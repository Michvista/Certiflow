Place the OSHA reference PDF in this folder as:

`osha-1926.pdf`

Expected path:

`services/ai-worker/src/rag/documents/osha-1926.pdf`

The AI worker will use that file when it exists. If it is missing, the worker falls back to a short built-in excerpt so the service can still start.
